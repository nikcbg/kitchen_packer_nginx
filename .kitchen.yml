---
driver:
  name: vagrant

provisioner:
  name: shell

platforms:
  - name: vbox/nginx64_2
    driver:
      box: nginx64_2
      provider: virtualbox
 
verifier:
  name: inspec

suites:
  - name: default
